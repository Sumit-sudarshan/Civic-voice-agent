# Set at build time; empty means same-origin (see api/client.js).
VITE_API_BASE_URL=

# Public site key from `gcloud recaptcha keys create` (backend/.env.example
# has the matching server-side RECAPTCHA_API_KEY). Safe to expose client-side.
VITE_RECAPTCHA_SITE_KEY=
